%li{:id=>"Entry#{entry.id}"}
  = entry.name
  %button{:onclick=>"$('#NewStepFormOf#{entry.id}').show();"}
    new step
  = link_to 'delete', entry_path(entry.id),:method=>'delete', :remote=>true,:confirm=>'are you sure?'
  %div
    = render 'steps', :entry=>entry

