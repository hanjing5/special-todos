%ul{:id=>"StepsOf#{entry.id}"}
  = render 'steps/step_form', :id => entry.id
  -entry.steps.each do |step|
    %li{:id=>"Step#{step.id}"}
      = step.name
      = link_to 'delete', entry_step_path(step.entry_id, step.id),:method=>'delete', :remote=>true,:confirm=>'are you sure?'
